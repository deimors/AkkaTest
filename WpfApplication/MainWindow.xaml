<Window x:Class="WpfApplication.MainWindow"
				xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
				xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
				xmlns:qc="http://QuickConverter.CodePlex.com/"
				xmlns:local="clr-namespace:WpfApplication"
				xmlns:controls="clr-namespace:WpfApplication.Controls"
				mc:Ignorable="d"
				Title="MainWindow" Height="350" Width="525">
	<Window.Resources>
		<SolidColorBrush Color="#FFABADB3" x:Key="NormalBorder"/>
		<SolidColorBrush Color="Red" x:Key="InvalidBorder"/>
	</Window.Resources>
	
	<Grid>
			<DockPanel>
				<DockPanel DockPanel.Dock="Top" LastChildFill="True">
					<Button DockPanel.Dock="Right" Content="Create" Click="{qc:QuickEvent '$dataContext.CreateIssue()'}"/>
					<controls:ValidatingTextBox Text="{Binding NewIssueTitle, UpdateSourceTrigger=PropertyChanged}" ValidationMessage="{Binding CreateIssueError}" />
				</DockPanel>
				<ListBox DockPanel.Dock="Bottom" ItemsSource="{Binding Issues}">
					<ListBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Title}" />
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
			</DockPanel>
		</Grid>
</Window>
